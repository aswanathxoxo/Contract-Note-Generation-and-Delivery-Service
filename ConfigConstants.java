package com.gtl.constants;

public class ConfigConstants {

    public static final String TOPIC_REGEX="${topic.regex}";
    public static final String FILE_UPLOAD_PATH="${file.upload.path}";
    public static final String OUTPUT_FILE_PATH="${file.destination.path}";
    public static final String DB_SERVICE_URL = "${db.service.url}";
    public static final String IMG_SERVICE_URL = "${image.service.url}";
    public static  final String POSTPROCESS_SP_ID = "${post-process.sp.id}";
    public static  final String DEFAULT_DB_CONN = "${default.db.connection}";

    public static final String SMTP_AUTH = "${smtp.auth}";
    public static final String SMTP_STARTTLS = "${smtp.starttls}";
    public static final String SMTP_HOST = "${smtp.host}";
    public static final String SMTP_PORT = "${smtp.port}";
    public static final String SMTP_USERNAME = "${smtp.username}";
    public static final String SMTP_PASSWORD = "${smtp.password}";
}

